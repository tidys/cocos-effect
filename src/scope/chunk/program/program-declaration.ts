import { Interval } from "../../interval";
import { Scope } from "../../scope";

export class ProgramDeclaration {
    constructor(name: string, nameInterval: Interval | null, scope: Scope) {
        // super(name, nameInterval, scope);
    }
}